# Todoist Export

Simple Golang application to export Todoist data (Placeholder).

## Build & Run

```bash
go run .
```

## Docker

```bash
docker build -t todoist-export .
docker run -p 8080:8080 todoist-export
```

## CI/CD

Uses GitHub Actions to build and push to GHCR.
